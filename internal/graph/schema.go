package graph

// GraphName is the name of the Apache AGE graph instance that stores all
// Context0 nodes (Project, Memory, Session) and their relationships.
// This constant is referenced in every Cypher query executed via AGERepository.
const GraphName = "context0"
